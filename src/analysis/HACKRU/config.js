// Configuration file for Google Generative AI
// API keys are now handled server-side for security

export const config = {
  // Google API key is now handled server-side for security
  // Available models: gemini-pro, gemini-pro-vision, etc.
  geminiModel: 'gemini-2.5-flash',
  // ElevenLabs API key is now handled server-side via /api/elevenlabs route
};